# Used Python programming language, Following are required pacakges need to be installed before running:
1. PIL : used in ImageTranslation
2. skimage and cv2 : to read images 
3. tensorflow, keras, json : used in CNN
4. DETECTO : one of the FasterRCNN model
5. pytorch : used in FasterRCNN model
other packages used numpy, os, matplotlib
6.LabelImg to label into xml anotation

